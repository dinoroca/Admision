'use strict';

var User = require ('../Models/User');

const registro_user = async function(req, res) {
    //TODO: Desarrollar la función
    res.status(200).send({message: 'Hola mundo'});
}

module.exports = {
    registro_user
}